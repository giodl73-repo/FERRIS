# Pulse 46 public-result closeout

Status: Permanently `invalid-publication-integrity`, non-retryable, and
null-conclusion.
Blocker: `P46-PUBLICATION-INDETERMINATE` at
`public-result-publication`.

This is the canonical public closeout for Pulse 46's sole independent launch.
It is **not** the failed Pulse 43 transactional result. Pulse 43, Pulse 44,
and Pulse 45 releases are unchanged and remain available for future redesign.

## Public-safe basis

- Authority commit:
  `a80111845f942b75e985c412389bfe6a89ccdc99`
- Immutable cutoff:
  `22ea38e274b882d6e607810382f842b76e483f10`
- Declaration identity:
  `sha256:92847e645338fd142710c1afcff5d6ad5540c35e6322ccf59b574f2fd3d61534`
- Sole independent custodian statement:
  `Publication posture: indeterminate. The required final public-result directory is absent.`

The main workspace confirmed the required final
`docs/simulations/profile-diff-held-out/pulse-46-public-result` directory was
absent before this closeout was recorded. The closeout records one launch and
zero retries. It records no private runtime, gate, search, material, or
blocker detail; all ordered-gate attempts, completions, terminal-gate, and
search fields are indeterminate/null.

## Artifacts

- `public-result.json` is the canonical closeout envelope.
- `release-receipt.json` binds the canonical result payload and raw result
  bytes.

The required final transactional result files are absent (`1`), claimed or
observed result paths are `0`, and publication state is `indeterminate`.
