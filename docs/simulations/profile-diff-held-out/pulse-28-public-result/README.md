# Pulse 28 public result

Disposition: **invalid before candidates**.

The immutable public release binding audit failed before package copy, build, preflight, generation, or candidate execution. No adapter invocation, verifier process, FERRIS candidate process, pair seal, corpus, seed, classifier, generator, or reproducer was created.

The machine-readable receipt is `PULSE-28-PUBLIC-RESULT.json`. It records the authorized cutoff, public binding counts, zero execution counts, null category conclusion, and the prohibition on further launches.
