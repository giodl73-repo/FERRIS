# Pulse 30 Public Result

**Disposition:** `invalid`

Result raw SHA-256:
`sha256:f75d33f054002cdd1b066678163ef926f62ec95ba826fef7273bc614c348f090`

Receipt ID:
`sha256:8f08b0cf27f1b1bb97bcea0591b92c2143cf324736e2112744122838ca58dc30`

Normalization passed 36/36 attribute checks, 36/36 LF checks, and 76/76
normalized bindings. Exact package verification copied 20 files, recomputed
20 hashes and four aggregates, verified six report/receipt/seal bindings, and
found zero extra files. Both cutoff binaries and both environments were
frozen.

The one permitted preflight passed with one invocation, two pairs, four rows,
two seals, two fresh verifiers, whole-store cardinality `2/2/2`, zero retries,
and zero residue.

Generation then stopped at `generation-before-case-materialization` because
the authorized public read scope did not provide a public
`ferris.profile-evidence/v0` input schema. Constructing inputs without that
public rule would violate the public-only generator requirement; consulting
Ferris source or tests was prohibited. There were zero candidates, zero
candidate processes, zero retries, no corpus, no minimization, and no
reproducer.

The category conclusion is null. Further launches are prohibited. This result
grants no fix authority. Pulse 31 later publishes the missing public input
contract prospectively; it does not reopen this result.
