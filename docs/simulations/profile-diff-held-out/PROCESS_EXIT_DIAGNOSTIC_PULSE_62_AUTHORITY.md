# Pulse 62 independent witness-preserving capability/materialization diagnostic authority

Status: withdrawn historical authority; permanently `invalid-prelaunch-path-route-contract`
under `P62-REAL-PATH-WSL-ROUTE-CONTRACT`
Immutable self-excluding cutoff: `e38dd20f37923e84ac3a3377892c1a5d0954266a`
Historical declaration identity: `sha256:f0db3ddf18a796d0ec107d6d73e9a08cf5e59d47cdad880d584ee8c7e8f61c5a`

Pulse 62 is now permanently withdrawn before launch. It remains the exact
historical prelaunch authority artifact; no declaration, schema, mutation,
source, runtime, seed, descriptor, result, or witness artifact is amended.

## Historical authority

Pulse 62 was one fresh independent diagnostic authority. It was not a retry,
resume, amendment, reconstruction, reseed, reuse, correlation, or inference of
any permanently closed predecessor. It bound exact final Pulse 59 HEAD
`6945f5fc96868c97267a1635fbb5219cc398eeb4`, the complete exact P27/P31/P35/
P37/P39/P41/P43/P47/P51/P52/P56/P57/P58/P59 release chain, immutable cutoff
Git-blob identities plus declared complete-file LF/CRLF variants, mandatory
reversible creatability probes for the exact private-runtime/P41/P59/Ubuntu
parents, one exact Pulse 59 production call, path-free terminal custody
transfer, null-conclusion `not-attempted` closeout, and fatal unresolved-
custody cleanup posture.

## Permanent predecessor closure

Pulse 46 and Pulse 48 remain permanently `invalid-publication-integrity`.
Pulse 49 remains permanently withdrawn `invalid-prelaunch-authority-integrity`;
Pulse 50 remains permanently withdrawn
`invalid-prelaunch-infrastructure-integrity`; Pulse 54 remains permanently
withdrawn `invalid-prelaunch-checkout-variant-integrity` under
`P54-CHECKOUT-VARIANT-VALIDATOR-INTEGRITY`; Pulse 55 remains permanently
closed `terminal-prerequisite-identity-failure` under
`P55-P33-RETAINED-IDENTITY-CONTRACT`; Pulse 60 remains permanently withdrawn
`invalid-prelaunch-runtime-root-contract` under
`P60-RUNTIME-ROOT-CALLABLE-CONTRACT`; and Pulse 61 remains permanently
withdrawn `invalid-prelaunch-root-creatability-contract` under
`P61-ROOT-CREATABILITY-CALLABLE-CONTRACT`. Pulse 62 does not revive,
reinterpret, or consume any of those historical closures.

## Independent prelaunch path/route review (2026-08-16)

The exact cutoff Pulse 59, Pulse 58, Pulse 57, Pulse 56, Pulse 52, Pulse 41,
Pulse 39, and Pulse 51 helpers were re-audited without invoking any authority
or diagnostic callable. That review proved the sealed Pulse 62 pre-call
contract still contradicted the exact one-call stack in two ways.

### 1. Real path qualification remained underbound

Pulse 62 added generic reversible probes, but those probes still did not use
the actual caller-supplied basenames or the deepest real relative paths/names
that the bound helpers can create:

- the Pulse 41 probe used a synthetic `pulse62-probe-*` final root plus
  `payload/probe.marker`, rather than the actual caller-supplied
  `p41_final_root.name`, its derived `.<final-root-name>.pulse-41-stage` stage
  root, rollback cleanup of that same final root, and the deepest real Pulse
  39 relative path copied by the exact transactional release;
- the Pulse 59 terminal probe used a generic
  `pulse62-probe-...pulse59-terminal-publication.probe` child, not the actual
  `<private_runtime_root.name>.pulse59-terminal-publication` sibling that the
  exact terminal wrapper derives; and
- the Pulse 56/Pulse 57 Ubuntu probes proved only shorter probe names, not an
  exact-length conservative nested topology under the real parent basenames or
  an explicit measured worst-case Cargo checkout/target/build path bound where
  that bound can be proven deterministically.

Because those public probes were shorter than real callable paths, Pulse 62
could not prove path-length headroom for every actual create/rename/remove
operation before launch.

### 2. Exact harmless WSL gate-3 route preflight was absent

Pulse 62 also did not bind the exact harmless WSL route preflight required to
prove the native Ubuntu gate-3 path before any real capability build or FERRIS
execution. It did not require one bounded call that resolves Windows `wsl.exe`,
invokes distribution exactly `Ubuntu-24.04` and exact `/usr/bin/python3`, runs
only an isolated harmless script, revalidates native `ubuntu_runtime_parent`
outside `/mnt/*`, proves platform/python identity, creates/fsyncs/removes the
exact-shaped Pulse 57/Pulse 56 probe children, emits only canonical bounded
JSON, and obeys explicit command/environment/output/no-retry limits. Other
exact gate-3 route prerequisites therefore remained only partially audited.

No Pulse 59 callable was invoked. No direct Pulse 58, Pulse 57, Pulse 56,
Pulse 47, Pulse 43, Pulse 41, Pulse 39, or Pulse 27 callable was invoked. No
runtime root, probe tree, seed, descriptor, candidate process, publication
root, result tree, witness tree, or transfer artifact was created.

## Permanent closeout

Pulse 62 is permanently withdrawn before launch under
`P62-REAL-PATH-WSL-ROUTE-CONTRACT`. Every call, seed, descriptor, process,
publication, transfer, result, and witness count remains zero. Category,
diagnostic, and product conclusions remain null. Pulse 62 is non-retryable,
non-resumable, and cannot be amended, reinterpreted, or consumed. Any
successor must use a new immutable cutoff containing this withdrawal and exact
final Pulse 59.

## Exhaustive control surface

The historical declaration, recursive Draft 2020-12 schema, and deterministic
mutation registry remain unchanged at `21644` controls. Pulse 63 later raised
the monotonic registry total to `184635`.

See the [Pulse 62 wave record](../../../context/waves/2026-08-12-platform-profile-conformance/pulses/pulse-62.md).
