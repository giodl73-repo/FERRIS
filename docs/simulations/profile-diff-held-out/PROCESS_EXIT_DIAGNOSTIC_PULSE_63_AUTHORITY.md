# Pulse 63 independent witness-preserving capability/materialization diagnostic authority

Status: withdrawn historical authority; permanently `invalid-prelaunch-wsl-bootstrap-contract`
under `P63-P57-EXACT-WSL-BOOTSTRAP-CONTRACT`
Immutable self-excluding cutoff: `5ad78a0623611ad57797ec4e9da34345b40a6e38`
Historical declaration identity: `sha256:b8cfea5cc8cb6dc52a7974f4fee35f6351557158943cc92af388c534421915d5`

Pulse 63 is now permanently withdrawn before launch. It remains the exact
historical prelaunch authority artifact; no declaration, schema, mutation,
source, runtime, seed, descriptor, result, or witness artifact is amended.

## Historical authority

Pulse 63 was one fresh independent diagnostic authority. It was not a retry,
resume, amendment, reconstruction, reseed, reuse, correlation, or inference of
any permanently closed predecessor. It bound exact final Pulse 59 HEAD
`6945f5fc96868c97267a1635fbb5219cc398eeb4`, the complete exact P27/P31/P35/
P37/P39/P41/P43/P47/P51/P52/P56/P57/P58/P59 release chain, immutable cutoff
Git-blob identities plus declared complete-file LF/CRLF variants, mandatory
actual-basename and conservative exact-topology public qualification for the
exact private-runtime/P41/P56/P57/P58/P59 stack, one exact harmless WSL route
preflight, one exact Pulse 59 production call, path-free terminal custody
transfer, null-conclusion `not-attempted` closeout, and fatal unresolved-
custody cleanup posture.

## Permanent predecessor closure

Pulse 46 and Pulse 48 remain permanently `invalid-publication-integrity`.
Pulse 49 remains permanently withdrawn `invalid-prelaunch-authority-integrity`;
Pulse 50 remains permanently withdrawn
`invalid-prelaunch-infrastructure-integrity`; Pulse 54 remains permanently
withdrawn `invalid-prelaunch-checkout-variant-integrity` under
`P54-CHECKOUT-VARIANT-VALIDATOR-INTEGRITY`; Pulse 55 remains permanently
closed `terminal-prerequisite-identity-failure` under
`P55-P33-RETAINED-IDENTITY-CONTRACT`; Pulse 60 remains permanently withdrawn
`invalid-prelaunch-runtime-root-contract` under
`P60-RUNTIME-ROOT-CALLABLE-CONTRACT`; Pulse 61 remains permanently withdrawn
`invalid-prelaunch-root-creatability-contract` under
`P61-ROOT-CREATABILITY-CALLABLE-CONTRACT`; and Pulse 62 remains permanently
withdrawn `invalid-prelaunch-path-route-contract` under
`P62-REAL-PATH-WSL-ROUTE-CONTRACT`. Pulse 63 does not revive,
reinterpret, or consume any of those historical closures.

## Independent prelaunch exact WSL bootstrap review (2026-08-16)

The exact cutoff Pulse 59, Pulse 58, Pulse 57, Pulse 56, Pulse 52, Pulse 41,
Pulse 39, Pulse 51, Pulse 57 `wsl_session_worker.py`, and Pulse 57 sealed
source loader were re-audited without invoking any authority or diagnostic
callable. That review proved the sealed Pulse 63 pre-call contract still
contradicted the exact Pulse 57 `_NativeWslSession` construction path in two
ways.

### 1. Exact Pulse 57 bundle-bootstrap route remained underbound and partially incorrect

Pulse 63 bound only a generic WSL route prefix plus synthetic probe outputs.
The exact cutoff Pulse 57 source instead requires all of the following before
one native Ubuntu worker can exist:

- Windows `%SystemRoot%\\System32\\wsl.exe` derived only through exact
  `_wsl_executable()` plus exact `_wsl_environment()` values for `ComSpec`,
  `PATH`, and `SystemRoot`;
- exact `--distribution Ubuntu-24.04 --exec /usr/bin/python3 -I -S -B -c`
  command shape with inline `_WSL_BUNDLE_BOOTSTRAP` and post-bootstrap argv
  `(runtime_parent, name)`;
- one canonical input payload with schema `ferris.pulse-57-wsl-bundle/v1`
  bounded by `1048576` bytes, not a smaller generic envelope;
- exact bundle-name contract `.p57-` plus 32 lowercase hex characters, slash
  forbidden, and exact derived root
  `ubuntu_runtime_parent.rstrip("/") + "/" + name`;
- exact staged file set `worker/wsl_session_worker.py`,
  `worker/sealed_dependencies.py`, and the complete exact Pulse 56 release
  under actual
  `repository/docs/simulations/profile-diff-held-out/pulse-56-retained-build-custody-release/`;
- exact canonical stdout fields `bundle_root`, `python`, and `schema`, no
  stderr, and exact source-bounded protocol size/timeout rules; and
- exact public confirmation that the emitted `bundle_root` equals the derived
  caller-parent-plus-name formula above.

Pulse 63 instead declared `16384` input bytes, `65536` result bytes, a generic
`60` second timeout, path-bearing `bundle_probe_root` / `p56_probe_root`
result fields, and the misleading deepest staged repository path
`repository/docs/simulations/profile-diff-held-out/pulse-59-witness-preserving-capability-materialization-executor-release/schemas/ferris.pulse-59-witness-preserving-capability-materialization-executor.v1.schema.json`.
That path is not staged by `_p56_bundle_files`; the actual staged repository
set is the exact Pulse 56 release tree.

### 2. Exact worker-bootstrap, source-loader, and private-parent binding remained under-audited

Pulse 63 also did not fully bind the exact second WSL bootstrap and source-
loader assumptions that `_NativeWslSession` and `wsl_session_worker.py`
require before a live 69-use capability can exist:

- exact `-c` `_WSL_WORKER_BOOTSTRAP` argv ordering for
  `worker/wsl_session_worker.py`, `WSL_WORKER_SHA256`, `--runtime-parent`,
  `ubuntu_runtime_parent`, `--bundle-root`, `staged.root`, `--p56-root`, and
  exact staged `repository/.../pulse-56-retained-build-custody-release`;
- exact descriptor-open/hash/compile execution of the worker from
  `/proc/self/fd/<descriptor>` with no worker-path reopen;
- exact bundled `sealed_dependencies.py` loading and worker-side proof that
  `p56_root == bundle_root / "repository" / ... /
  "pulse-56-retained-build-custody-release"` and `repo_root =
  p56_root.parents[3]`; and
- a custodian-private preflight record that may hold and compare the actual
  caller-supplied `ubuntu_runtime_parent` path while the durable public record
  emits only bounded status or digest material and never the path-bearing probe
  roots.

Because those route and privacy invariants were not fully sealed, Pulse 63
could not prove exact gate-3 route integrity before the sole Pulse 59 call.

No Pulse 59 callable was invoked. No direct Pulse 58, Pulse 57, Pulse 56,
Pulse 47, Pulse 43, Pulse 41, Pulse 39, or Pulse 27 callable was invoked. No
runtime root, probe tree, seed, descriptor, candidate process, publication
root, result tree, witness tree, or transfer artifact was created.

## Permanent closeout

Pulse 63 is permanently withdrawn before launch under
`P63-P57-EXACT-WSL-BOOTSTRAP-CONTRACT`. Every call, seed, descriptor, process,
publication, transfer, result, and witness count remains zero. Category,
diagnostic, and product conclusions remain null. Pulse 63 is non-retryable,
non-resumable, and cannot be amended, reinterpreted, or consumed. Any
successor must use a new immutable cutoff containing this withdrawal and exact
final Pulse 59.

## Exhaustive control surface

The historical declaration, recursive Draft 2020-12 schema, and deterministic
mutation registry remain unchanged at `23266` controls. The monotonic registry
total remains `184635` until a separately sealed successor is added.

See the [Pulse 63 wave record](../../../context/waves/2026-08-12-platform-profile-conformance/pulses/pulse-63.md).
