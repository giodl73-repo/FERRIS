# Pulse 08: Private Enterprise Application Readiness Shadow

Status: Complete

## User outcome

Demonstrate that the implemented application-readiness path prevents one
workspace result from being presented as complete readiness for the
enterprise-shaped multi-workspace application that motivated the contract.

## Control record

- Product outcome: test APP-READINESS-001 against the custody-bound `EO-02`
  multi-workspace shape before considering onboarding or another product layer.
- Work completed: explicit workspace readiness caught missing Cargo in both
  enterprise consumers, and APP-READINESS-001 is implemented over frozen public
  fixtures.
- Value obtained: Ferris can now compose independent workspace observations,
  but only synthetic implementation evidence exists.
- Remaining risk: the explicit application binding, aggregate, privacy, and
  removal behavior have not been observed against the private enterprise shape.
- Pulses consumed: seven environment-readiness pulses; no prior private
  application-composition attempt.
- Proposed next action: one disposable local Windows shadow over `EO-02`.
- Product Value Governor: `continue-within-budget`.

## Authority

The user's fresh `go` after Pulse 07 authorizes one local Windows, read-only
application-readiness shadow over the custody-bound private enterprise consumer
identified publicly only as `EO-02`:

- build Ferris at exact public revision
  `9aeb5c55318ce7899c55d3cece15d09f6ee5c4b1`;
- bind the exact privately retained `EO-02` revision and require a clean source
  tree;
- create one disposable local clone and add only temporary evaluation
  application-readiness inputs to that clone;
- derive the three workspace IDs and manifest paths only from the existing
  owner topology record;
- run the aggregate first under the inherited missing-Cargo environment and
  then with the existing Cargo directory visible only to the Ferris process;
- require one blocked child to make the application blocked, require every
  child ready before application success, repeat the ready observation, and
  verify deterministic identities;
- verify no Cargo candidate or owner command runs, no private path or
  environment value enters retained output, and the source consumer remains
  unchanged; and
- remove the disposable clone and every raw input and output before retaining
  only public-safe aggregate evidence.

## Maximum effort

One exact build, one private consumer, one disposable clone, two environment
states, one deterministic repeat, one public-safe result, one eleven-role
review, and one commit. No product, schema, dependency, or adopter change.

## Completion test

- public and private revisions are exactly bound;
- source and disposable tracked trees start clean;
- inherited execution returns `blocked`, exit 7, with exactly one Cargo-missing
  child and two ready children;
- process-local Cargo visibility returns `success`, exit 0, with all three
  children ready;
- the ready repeat is byte-identical and preserves report, selection,
  invocation, and result identities;
- no owner command or candidate executable runs;
- outputs contain no private root, session path, username, machine name,
  resolved executable path, or process `PATH`;
- all temporary inputs, outputs, and the disposable clone are removed;
- the private source tree and public Ferris worktree remain clean apart from
  this pulse's public records; and
- targeted readiness tests, Markdown checks, and `git diff --check` pass.

## Stop condition

Stop on a revision mismatch, dirty source, failed exact build, invalid temporary
declaration, unexpected result, privacy leak, candidate or owner execution,
source mutation, or cleanup failure. Do not repair an environment, change
product behavior, alter the private consumer, run owner validation, or retry
with changed inputs inside this pulse.

## Result

Exact public Ferris revision
`9aeb5c55318ce7899c55d3cece15d09f6ee5c4b1` and the privately retained
`EO-02` revision were bound before execution. The source tree was clean, and
one disposable local clone contained exactly the three owner-declared workspace
roots plus five temporary evaluation inputs.

Under the inherited Windows environment, two children were ready and the one
child requiring Cargo was blocked. APP-READINESS-001 returned `blocked`, exit
7. With the existing Cargo directory visible only to the Ferris process, all
three children and the application returned ready, exit 0. The ready repeat was
byte-identical and preserved report, selection, invocation, and result
identities.

The privacy scan passed, no owner command or Cargo candidate ran, neither
tracked tree changed, and the disposable clone, temporary inputs, and raw
outputs were removed. Only a private custody summary remains outside the
repository. Findings `FERRIS-799` through `FERRIS-802` are recorded in the
[public-safe result](../../../../docs/research/2026-09-19-private-enterprise-application-readiness-shadow.md).
The
[eleven-role review](../../../../docs/plans/reviews/FERRIS_PRIVATE_ENTERPRISE_APPLICATION_READINESS_SHADOW_REVIEW.md)
accepts this bounded Windows evidence.

Pulse 08 is complete and exhausted. No product, schema, dependency, source
consumer, owner command, persistent environment, Linux rerun, adoption,
support, production, performance, savings, or successor authority follows.
